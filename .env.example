# Copy to .env and fill in to enable live Grok responses.
# Without XAI_API_KEY the app runs in offline mock mode.
XAI_API_KEY=
# Optional overrides:
# XAI_MODEL=grok-2-latest
# XAI_API_BASE=https://api.x.ai/v1
# PORT=5000
